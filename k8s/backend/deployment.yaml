apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: ghcr.io/whyredfire/ss-backend:ab12d83c1eeecfdb9c8df1564485425a128478b5
          ports:
            - containerPort: 8000
          env:
            - name: DB_CONNECTION
              valueFrom:
                configMapKeyRef:
                  name: backend-config
                  key: DB_CONNECTION
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: auth-secret
                  key: JWT_SECRET
            - name: PASS_SALT
              valueFrom:
                secretKeyRef:
                  name: auth-secret
                  key: PASS_SALT
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
